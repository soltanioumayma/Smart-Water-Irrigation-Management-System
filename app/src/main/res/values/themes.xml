<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Smart_water_projet" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Smart_water_projet" parent="Base.Theme.Smart_water_projet" />

    <!-- Accueil immersif (barres système translucides, rendu plus « premium ») -->
    <style name="Theme.Welcome" parent="Theme.Smart_water_projet">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">#050A12</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>