<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="22dp"
    android:background="@color/bg_dark_card">

    <ImageView
        android:id="@+id/ivPumpSummaryIcon"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center_horizontal"
        android:contentDescription="@null"
        android:src="@drawable/ic_water_drop"
        app:tint="@color/brand_teal"
        android:layout_marginBottom="10dp" />

    <TextView
        android:id="@+id/tvPumpSummaryTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/pump_summary_title"
        android:textColor="@color/text_white"
        android:textSize="19sp"
        android:textStyle="bold"
        android:layout_marginBottom="6dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:text="@string/pump_summary_subtitle"
        android:textColor="@color/text_hint_light"
        android:textSize="13sp"
        android:layout_marginBottom="14dp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:background="@drawable/bg_header_gradient"
        android:layout_marginBottom="14dp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="240dp"
        android:layout_marginBottom="16dp"
        android:fillViewport="true">

        <TextView
            android:id="@+id/tvPumpSummaryBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:lineSpacingExtra="4dp"
            android:textColor="@color/text_hint_light"
            android:textSize="14sp" />
    </ScrollView>

    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnPumpSummaryClose"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/pump_summary_close"
        android:textAllCaps="false"
        app:cornerRadius="12dp" />

</LinearLayout>
